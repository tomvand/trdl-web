[project]
name = "trdl-web"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tomvand", email = "tomvand@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
trdl-web = "trdl_web:main"

[build-system]
requires = ["uv_build>=0.9.28,<0.10.0"]
build-backend = "uv_build"
